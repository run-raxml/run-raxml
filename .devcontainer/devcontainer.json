{
    "name": "RaXML Development Container",
    "build": {
        "dockerfile": "Dockerfile"
    },
    "postCreateCommand": "git clone https://github.com/stamatak/standard-RAxML /bin/ && cd /bin/standard-RAxML && make -f Makefile.AVX2.PTHREADS.gcc"
}

}
